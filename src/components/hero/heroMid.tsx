import { component$ } from "@builder.io/qwik"

export const HeroMid = component$(() => {
  return (
    <div class="py-10  overflow-scroll m-scroll relative overflow-x-scroll">
      <h1 class=" text-9xl font-bold whitespace-nowrap text-stroke text-transparent m-scroll__title">
        WEIGHT LOSS / MUSCLE GAIN / HEATHLY LIFESTYLE
      </h1>
    </div>
  )
})
